using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Loadsta : MonoBehaviour
{
    public Charsta stats;
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Health");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
